# Summit Sensory Gym — Customer Portal Process Flow & System Reference

**Version 1.0 — July 2026**
**Purpose:** A single source-of-truth reference for how the Customer Portal works end to end, from the moment a customer signs the proposal to the moment they receive delivery. Every step names the exact system and data source (Monday board + column ID, API endpoint, external service, or email) so this file can be re-uploaded to Claude to request precise changes.

---

## 0. How to use this document (read me first)

This document is written so you can upload it back into Claude and say things like:

- *"Add a new step after **S-4.3** that…"*
- *"Change the data source for **DS-12** from column X to column Y…"*
- *"Delete email **EM-07** and replace it with…"*
- *"The **Therapy Mats & Padding** shipment (see **SHP-2**) should now pull from a subitem instead of a column."*

Every stage, data source, endpoint, email, and screen has a **stable reference code** (e.g. `S-3.2`, `DS-05`, `EP-11`, `EM-04`, `SCR-2`). When you want something added, changed, or deleted, cite the code and describe the change. Claude will use the codes to locate the exact place in the code and this document.

**Source of truth for IDs:** the live column IDs come from `lib/monday.js` (the `COLS` map). If `SETUP.md` and `lib/monday.js` ever disagree, `lib/monday.js` wins.

**Legend for status of each item:**
`[LIVE]` = built and running · `[PARTIAL]` = built but not fully wired/configured · `[PLANNED]` = agreed but not yet built.

---

## 1. System architecture (the moving parts)

| Ref | System | Role | Where it lives |
|---|---|---|---|
| SYS-1 | **Next.js web app** (React) | The portal itself — customer UI + admin UI | Hosted on Vercel; code in `/pages`, `/lib` |
| SYS-2 | **Monday.com** | System of record for orders, status, files, tracking | Board: **Manufacturing Process**, Board ID `6533700776` |
| SYS-3 | **Jotform** | Color/product selection + document collection forms | Webhook → SYS-1 |
| SYS-4 | **FedEx Track API** | Live parcel/freight tracking status | Called by SYS-1 (`lib/fedex.js`) |
| SYS-5 | **Resend** | All transactional email | Called by SYS-1 (`lib/email.js`) |
| SYS-6 | **Microsoft 365 / Azure AD** | Staff login (admin portal) | NextAuth SSO |
| SYS-7 | **Vercel Cron** | Scheduled reminder engine | `pages/api/cron/reminders.js` |
| SYS-8 | **AfterShip** `[LIVE]` | Carrier-agnostic tracking engine (freight + parcel) | `lib/aftership.js`; webhook receiver at EP-21 syncs live status back onto Monday for accessory subitems (see PLAN-2) |

**Two audiences, two logins:**

- **Customers** log in at `/portal` with a one-time email code (no password). Session = signed JWT cookie `summit_customer_session` (7-day expiry). See EP-1/EP-2.
- **Staff** log in at `/admin` with Microsoft 365 SSO (any `@summitsensorygym.com` account). Route protection: `middleware.js`.

---

## 2. Data dictionary — Monday.com "Manufacturing Process" board (ID `6533700776`)

All column IDs are read in `lib/monday.js`. Many are overridable by environment variables (shown where relevant). Columns marked **mirror (read-only)** are sourced from connected boards and cannot be written by the portal.

| Ref | Field (portal name) | Monday column title | Column ID | Type | Written by | Notes |
|---|---|---|---|---|---|---|
| DS-01 | Customer email | Email Address | `email__1` | Email | — | Match key: customer ↔ order |
| DS-02 | **Order status / stage** | Manufacturing Phase | `status__1` | Status | Staff (in Monday) | **Drives the portal progress bar** (see S-flow & SCR-2) |
| DS-03 | Frame tracking # | GB FedEx Tracking Number | `lookup_mm1kcbb5` | Mirror (read-only) | Connected board | Frame shipment tracking (SHP-1) |
| DS-04 | Freight tracking ID | Freight Tracking ID | `dup__of_gb_production7__1` | Mirror (read-only) | Connected board | Alternate freight tracking source |
| DS-05 | Portal files | Portal Files | `file_mm4wbdrh` | File | Staff / admin portal | Files shared to customer (SCR-8) |
| DS-06 | Invoice link | Link to Customer Invoice | `text_mm4wfamc` | Text/URL | Staff | Shown in Invoice tab (SCR-9) |
| DS-07 | Payment link | Payment Portal URL | *(env `MONDAY_COL_PAYMENT_LINK`, unset)* | Text/URL | Staff | `[PARTIAL]` not yet mapped |
| DS-08 | Ship date | Initial Projected Ship Date | `date_mkvvpex1` | Date | Staff | Shown on Status tab |
| DS-09 | Delivery address | Confirmed Delivery Address | `long_text_mkpkdtj4` | Long text | Customer (via portal) | Writable; updated in Delivery tab. `[BUG FIXED 2026-07-28]` Every write to this column (and the `pages/api/monday/order.js` Contact-tab address PATCH) sent a bare string, but Monday's `long_text` columns require the complex value wrapped as `{text: "..."}` — the bare string threw `"invalid value"` and surfaced to customers as "Error saving. Please try again." on the Delivery & Site Details / Freight Acknowledgment step. Found via a live Vercel runtime error on `/api/portal/setup`. |
| DS-54 | Bill-To Address (portal-submitted) | Confirmed Bill-To Address (Portal) | `long_text_mm5a1xck` (`COLS.billingAddressConfirmed`) | Long text | Customer (via portal) | Written instantly on Billing Information submit (no staff review step); takes precedence over the read-only Deals-board mirror (DS-39) for display and as the default ship-to address on Delivery Logistics. Created 2026-07-16. `[BUG FIXED 2026-07-28]` Same bare-string-vs-`{text:...}` bug as DS-09 — **this one was NOT wrapped in `.catch`**, so every Billing Information submission since this column was created (2026-07-16) has been throwing "Error saving. Please try again." and failing outright, not just silently under-saving. This is almost certainly why customers' Delivery tab ship-to address had nothing to default from — the Billing submission that was supposed to populate it never actually completed. `[SECOND BUG FIXED 2026-07-28]` Because this column stores one combined string (street+city+state+zip+country all joined), `BillingTab` was dumping the whole thing into just the Street Address input on any repeat visit, leaving the required City/Country fields blank and risking a doubled-up address on resave. Fixed: the form now shows the confirmed address as read-only reference text and leaves inputs blank for clean re-entry instead of splicing it into one field. |

**Same bug class, 6 call sites total, all fixed 2026-07-28:** DS-09, DS-54 above, plus 3 long_text columns on the Delivery & Site Details Submissions board (SUB-6 Special Instructions, SUB-16 Full Ship-To Address Formatted, SUB-18 Changes Requiring Staff Confirmation — these failed silently, wrapped in `.catch`) and the Referrals board's Message column (REF-8, `long_text_mm592qp0` — also silent). Root cause: Monday's `long_text` column type requires the complex value wrapped as `{text: "..."}`; a bare string throws `"invalid value"`. Every other column type used elsewhere in this codebase (`text`, `status`, `date`, `checkbox`, `numbers`, `link`) was already using the correct shape — only `long_text` was wrong, everywhere it was used.
| DS-10 | Product type | Product Series STD Column | `color_mkvw7b8` | Status | Staff | Used for Jotform form matching |
| DS-11 | Mat/pad tracking | *(env `MONDAY_COL_MAT_TRACKING`, unset)* | — | Text | Staff | `[PARTIAL]` Feeds Mats & Padding card (SHP-2); to be replaced by subitem — see PLAN-2 |
| DS-12 | Other shipments *(retired 2026-07-14)* | *(env `MONDAY_COL_OTHER_SHIPMENTS`, unset)* | — | Text | Staff | `[DEPRECATED]` Superseded by DS-41…45 (Monday subitems) — see SHP-3, PLAN-4. Still parsed into `order.otherShipments` for backward compatibility but no longer rendered in the portal. |
| DS-13 | Installation links | *(installation resources)* | `text_mm4x6xvf` | Text | Staff | Format `Label\|URL` per line (SCR-7) |
| DS-14 | Installation videos | *(env `MONDAY_COL_INSTALLATION_VIDEOS`, unset)* | — | Text | Staff | `[PARTIAL]` YouTube/Vimeo URLs |
| DS-15 | Installation docs | *(env `MONDAY_COL_INSTALLATION_DOCS`, unset)* | — | Text | Staff | `[PARTIAL]` `Label\|URL` per line |
| DS-16 | Color form (direct) | Color Selection Form URL | `text_mm4y44dt` | Text | Staff | Preferred source for Jotform color-form ID |
| DS-17 | Color form (mirror) | *(legacy)* | `lookup_mm4xws1a` | Mirror (read-only) | Connected board | Fallback for DS-16 |
| DS-18 | Phone | POC Phone | `lookup_mkwaee43` | Mirror (read-only) | Connected board | |
| DS-19 | POC name | Delivery POC Name | `lookup_mkwb5bty` | Mirror (read-only) | Connected board | |
| DS-20 | POC email | POC Email | `lookup_mkwazctw` | Mirror (read-only) | Connected board | |
| DS-21 | First name | First Name | `lookup_mkvx85hs` | Mirror (read-only) | Connected board | Used in email greetings |
| DS-22 | Delivery instructions | Special Delivery Instructions | `lookup_mm0anh5a` | Mirror (read-only) | Connected board | |
| DS-23 | Balance due | *(env `MONDAY_COL_BALANCE`, unset)* | — | Numbers | Staff | `[PARTIAL]` not yet mapped |
| DS-33 | Tax exemption cert file | Tax Exemption Cert | `file_mm55t6kn` (`COLS.taxExemptCertFile`) | File | Customer (via portal) | `[LIVE — restored 2026-07-28]` Uploaded via EP-6 (`tax_exemption`) using the multipart `/v2/file` upload flow (`uploadFileToColumn()`); see PLAN-8 |
| DS-34 | Tax exemption status | Tax Exempt? | `color_mm55tjn2` (`COLS.taxExemptStatus`) | Status | Customer (via portal) | `[LIVE — restored 2026-07-28]` Yes/No — written via EP-6 (`tax_exemption`); see PLAN-8 |
| DS-39 | Setup progress checklist | Portal: Contact/Billing/Delivery/Colors/Documents | `color_mm4ybgaa`/`color_mm51e5w9`/`color_mm51j15w`/`color_mm51hjph`/`color_mm51yqbz` (`order.progress`) | Status ×5 | Staff (view) | `[LIVE 2026-07-27]` These 5 columns already existed (written by `markSectionComplete`) but weren't surfaced anywhere in the Admin portal. Now shown as a 5-dot readout on Orders and Customers tabs (`ProgressDots`) so staff can see what a customer needs help finishing before calling them. |
| DS-40 | Message queue status | Message Status | `color_mm5pny4b` (`COLS.messageStatus`) | Status | Staff (view) | `[LIVE 2026-07-27, FIXED 2026-07-28]` New column, created via monday.com MCP. Flips to **Needs Reply** when a customer sends a portal message (EP-12). Flips back to **Replied** via two paths: (1) staff reply from the **Admin Portal Messages tab** — `pages/api/monday/messages.js` now sets this directly on the staff-POST branch (bug found + fixed 2026-07-28: this branch previously only existed for customers, so an Admin Portal reply never cleared the flag); (2) staff reply **directly in Monday.com's Updates feed** — handled by `update-webhook.js` (EP-14), which requires a Monday automation ("When an update is created → send a webhook") that is **not currently registered** on this board — see OPEN-3. Admin Messages tab (SCR — Messages) sorts Needs-Reply threads to the top with a red dot; Messages nav tab shows a count badge. |
| DS-35 | Ship-to address (updated) | Confirmed Delivery Address | `long_text_mkpkdtj4` (= DS-09) | Long text | Customer (via portal) | Written only when customer answers "No" to the address-confirm question in Delivery & Site Details (S-2.3) |
| DS-36 | Primary Contact Name | *(mirror)* | `lookup6__1` | Mirror (read-only) | Connected board | Contact Information tab — Primary Contact (distinct from Delivery POC mirrors DS-18…20) |
| DS-37 | Primary Contact Email | *(mirror)* | `dup__of_contact8__1` | Mirror (read-only) | Connected board | Contact Information tab — Primary Contact |
| DS-38 | Primary Contact Phone | *(mirror)* | `dup__of_contact7__1` | Mirror (read-only) | Connected board | Contact Information tab — Primary Contact |
| DS-39 | Bill-To Address | *(mirror)* | `lookup18__1` | Mirror (read-only) | Connected board | Billing Information tab — pre-fills Bill-To Address (2026-07-13: replaced the "same as delivery" toggle) |
| DS-40 | Bill-To Zip/Postal Code | *(mirror)* | `dup__of_location__1` | Mirror (read-only) | Connected board | Billing Information tab — pre-fills Bill-To Address zip |
| DS-41 | Accessory item — Order Status | "Order Status" (subitem) | `color_mm58wahg` | Status | Staff | `[LIVE]` Order Pending / Ordered / Out of Stock — see SHP-3, PLAN-2 |
| DS-42 | Accessory item — Date Ordered | "Date Ordered" (subitem) | `date_mm58v7nv` | Date | Staff | `[LIVE]` Set when Order Status flips to Ordered |
| DS-43 | Accessory item — Freight Carrier (display) | "Freight Carrier" (subitem) | `color_mm51tf2w` | Status | Staff | `[LIVE]` Human-readable carrier label for staff only — **NOT** read by the app for AfterShip (corrected 2026-07-14; see DS-46) |
| DS-44 | Accessory item — Freight Tracking ID | "Freight Tracking ID" (subitem) | `text_mm5125q0` | Text | Staff | `[LIVE]` |
| DS-45 | Accessory item — Carrier Status | "Carrier Status" (subitem) | `color_mm58zsbq` | Status | AfterShip (EP-21 webhook, EP-23 cron) | `[LIVE]` Auto-written (create_labels_if_missing) as the shipment transits — In Transit / Out for Delivery / Delivered / Exception / etc. |
| DS-46 | Accessory item — Carrier Code | "Carrier Code" (subitem) | `text_mm58cnxf` | Text | Staff | `[LIVE]` **The actual AfterShip slug** (fedex / ups / usps / fedex-freight / etc.) — corrected 2026-07-14, was originally read from DS-43 in error |
| DS-47 | Photo/Video Showcase form ID | Photo/Video Showcase Form URL | `text_mm59x2cb` | Text | Staff | `[LIVE]` Same pattern as DS-16 (colorFormDirect) — paste the Jotform form URL/ID here once Bryan builds the "Share Your Gym" form. Also readable via `JOTFORM_SHOWCASE_FORM_ID` env override. Column created 2026-07-15; still needs the actual form URL pasted in once Bryan builds it. |
| DS-48 | UGC — Customer Photos | Customer Photos | `file_mm59ndjx` | File | Portal (via EP-16 showcase branch) | `[LIVE]` One file per submitted photo, written by `attachUgcFile()` |
| DS-49 | UGC — Customer Videos | Customer Videos | `file_mm599cvb` | File | Portal (via EP-16 showcase branch) | `[LIVE]` One file per submitted video, written by `attachUgcFile()` — kept in a **separate column from DS-48** per Bryan's requirement |
| DS-50 | UGC — Photo Count | UGC Photo Count | `numeric_mm59hr` | Number | Portal (via `incrementUgcCounts()`) | `[LIVE]` Running count — Monday can't count Files-column attachments natively (confirmed: Files columns are incompatible with Formula columns), so counting happens in app code |
| DS-51 | UGC — Video Count | UGC Video Count | `numeric_mm593cqk` | Number | Portal (via `incrementUgcCounts()`) | `[LIVE]` Running count, same mechanism as DS-50 |
| DS-52 | UGC — Reward Credits | UGC Reward Credits | `numeric_mm59p094` | Number | Portal (via `incrementUgcCounts()`) | `[LIVE]` `photoCount + videoCount × 2`; every 10 credits = one $25 reward tier |
| DS-53 | UGC — Reward Status | UGC Reward Status | `color_mm59p3gr` | Status | Portal (auto) + Staff (manual approval) | `[LIVE]` Labels: None / Pending Review / Approved / Paid. Flips to "Pending Review" automatically when a new reward tier is crossed (EM-16 to team); staff manually approves after a quick quality check (angles, usage, video ≥20s) — the app never auto-approves |

### 2a-0. Referrals board `[LIVE]`

Standalone Monday board — created 2026-07-15 (board ID `18422254966`, workspace "Summit Sensory Gym", `https://summit-sensory-gym.monday.com/boards/18422254966`) — that logs every "Refer a Friend" submission as its own row. Reward amount is computed by a **Monday Formula column** (Numbers columns are formula-compatible, unlike Files columns) once staff enters the referred friend's eventual order value: **2% of order value, $25 floor, $500 cap** (formula: `IF({Referred Order Value}=0,0,ROUND(MIN(MAX({Referred Order Value}*0.02,25),500),2))`). Default payout form is **account credit** (cash-equivalent gift cards for large/institutional referrals — clinics, schools, hospitals — carry kickback-perception risk); straight gift cards remain fine for smaller individual/family referrals at Bryan's discretion.

| Ref | Field | Monday column title | Column ID | Type |
|---|---|---|---|---|
| REF-BOARD | *(board)* | Referrals | `18422254966` | — |
| REF-1 | Referrer Name | Referrer Name | `text_mm59xen8` | Text |
| REF-2 | Referrer Email | Referrer Email | `text_mm59tasb` | Text |
| REF-3 | Referrer Order Item ID | Referrer Order Item ID | `text_mm597ngj` | Text |
| REF-4 | Referrer Order Record Link | Referrer Order Record Link | `link_mm59wm1q` | Link |
| REF-5 | Referred Friend Name | Referred Friend Name | `text_mm59ev00` | Text |
| REF-6 | Referred Friend Email | Referred Friend Email | `text_mm59gert` | Text |
| REF-7 | Referred Friend Phone | Referred Friend Phone | `text_mm59v1f2` | Text |
| REF-8 | Message (optional) | Message | `long_text_mm592qp0` | Long text |
| REF-9 | Submitted Date | Submitted Date | `date_mm59a5gn` | Date |
| REF-10 | Referred Order Value | Referred Order Value | `numeric_mm59x511` | Number *(staff-managed, not written by app)* |
| REF-11 | Reward Amount | Reward Amount | `formula_mm59bmwg` | Formula *(auto-computes 2% of REF-10, $25 floor/$500 cap)* |
| REF-12 | Reward Type | Reward Type | `color_mm59j1g8` | Status — Account Credit / Gift Card *(staff-managed)* |
| REF-13 | Referral Status | Referral Status | `color_mm59a5h4` | Status — New Lead / Contacted / Converted / Not Interested / Reward Issued *(staff-managed)* |

Written by `lib/monday.js` → `createReferralItem()`, called from EP-24 (`/api/referral/submit`). Board and column IDs are hardcoded defaults in `lib/monday.js` (same pattern as `ACCESSORY_BOARD_ID`/`DELIVERY_BOARD_ID`), overridable via env vars. See PLAN-9.

### 2a-1. Delivery & Site Details Submissions board `[LIVE]`

Standalone Monday board (not the Manufacturing Process board) that logs every Delivery & Site Details submission as its own row/item, so edits/resubmissions keep full history instead of overwriting one record. Created 2026-07-13 via the monday.com API. Board ID and every column ID default in code (`lib/monday.js` → `DELIVERY_BOARD_ID`, `DELIVERY_COLS`) but can be overridden with env vars without a code change.

| Ref | Field | Monday column title | Column ID (default) | Type |
|---|---|---|---|---|
| SUB-BOARD | *(board)* | Delivery & Site Details Submissions | `18421779422` | — |
| SUB-1 | Order Item ID | Order Item ID | `text_mm571ym4` | Text |
| SUB-2 | Order Record Link | Order Record Link | `link_mm57qepn` | Link |
| SUB-3 | Customer Email | Customer Email | `text_mm57c4dm` | Text |
| SUB-4 | Submitted Date | Submitted Date | `date_mm57s4r5` | Date |
| SUB-5 | Primary POC Name/Phone/Can-Text/Email | Primary POC Name/Phone/Can Text/Email | `text_mm57830j` / `text_mm5767gr` / `boolean_mm57h4hs` / `text_mm57qnte` | Text/Text/Checkbox/Text |
| SUB-6 | Special Delivery Instructions | Special Delivery Instructions | `long_text_mm57e4q` | Long text |
| SUB-7 | Has Secondary POC | Has Secondary POC | `text_mm571zcw` | Text |
| SUB-8 | Secondary POC Name/Phone/Can-Text/Email | Secondary POC Name/Phone/Can Text/Email | `text_mm57as86` / `text_mm576cps` / `boolean_mm57h4a5` / `text_mm57kmfe` | Text/Text/Checkbox/Text |
| SUB-9 | Primary Preferred Communication Method | Preferred Communication Method | `text_mm57t33w` | Text |
| SUB-10 | Primary Mobile Number for Texts | Mobile Number for Texts | `text_mm5778hh` | Text |
| SUB-20 | Secondary Preferred Communication Method | Secondary Preferred Communication Method | `text_mm572ns5` | Text |
| SUB-21 | Secondary Mobile Number | Secondary Mobile Number | `text_mm57wdc2` | Text |
| SUB-11 | Loading Dock at Facility | Loading Dock at Facility | `text_mm5712dx` | Text |
| SUB-12 | Delivery Timing Preference | Delivery Timing Preference | `text_mm57q2s6` | Text |
| SUB-13 | Preferred Delivery Date | Preferred Delivery Date | `date_mm57m9rp` | Date |
| SUB-14 | Ship-To Address Confirmed | Ship-To Address Confirmed | `text_mm572geh` | Text |
| SUB-15 | Address Line 1/2, City, State, Zip, Country | Address Line 1/2 / City / State or Province / ZIP or Postal Code / Country | `text_mm57sf21` / `text_mm57mbr7` / `text_mm57g87z` / `text_mm57hhxm` / `text_mm57wkbf` / `text_mm57n32a` | Text (×6) |
| SUB-16 | Full Ship-To Address Formatted | Full Ship-To Address Formatted | `long_text_mm57vhh3` | Long text |
| SUB-17 | Freight Ack Signed By / Date | Freight Ack Signed By / Freight Ack Date | `text_mm57nnck` / `date_mm578gmh` | Text/Date |
| SUB-18 | Changes Requiring Staff Confirmation | Changes Requiring Staff Confirmation | `long_text_mm57r7b1` | Long text |
| SUB-19 | Staff Reviewed | Staff Reviewed | `boolean_mm576c3b` | Checkbox |

Written by `lib/monday.js` → `createDeliverySubmissionItem()`, called from `EP-6 ('delivery')` in `pages/api/portal/setup.js`. Creates one new item per submission (does not overwrite prior submissions).

### 2a. Portal onboarding checklist columns `[LIVE]`

Status columns; label `✅` = complete, `🚫` = incomplete. The portal flips these to `✅` automatically when a customer finishes a section (see EP-6 and the S-2 stages).

| Ref | Portal section | Monday column title | Column ID |
|---|---|---|---|
| DS-24 | Contact | Portal: Contact | `color_mm4ybgaa` |
| DS-25 | Billing | Portal: Billing | `color_mm51e5w9` |
| DS-26 | Delivery & Freight Ack | Portal: Delivery & Freight Ack | `color_mm51j15w` |
| DS-27 | Color Selections | Portal: Color Selections | `color_mm51hjph` |
| DS-28 | Documents | Portal: Documents | `color_mm51yqbz` |

### 2b. Order status → progress-bar stages (DS-02 label map)

Defined in `lib/monday.js` `STATUS_STAGES` (labels overridable via env). The portal matches the current `Manufacturing Phase` label exactly to a stage; unmatched labels default to stage 0.

| Stage index | Stage key | Default Monday label | Icon |
|---|---|---|---|
| 0 | order_placed | Order Placed | 📋 |
| 1 | in_manufacturing | In Manufacturing | 🔧 |
| 2 | ready_to_ship | Ready to Ship | 📦 |
| 3 | shipped | Shipped | 🚚 |
| 4 | delivered | Delivered | ✅ |

### 2c. Monday "tagged updates" (audit trail written to the order's Updates feed)

The portal posts these as updates on the order item. The reminder engine (SYS-7) reads them to know what's complete and when the clock started.

`[PORTAL: Invitation Sent]` · `[PORTAL: Contact Confirmed]` · `[PORTAL: Contact Update Requested]` · `[PORTAL: Billing Information]` · `[PORTAL: Delivery Details]` · `[PORTAL: Freight Delivery Acknowledgment]` · `[PORTAL: Color Selections]` · `[PORTAL: Documents Submitted]` · `[PORTAL: Reminder #N]` · `[PORTAL: Tax Exempt - No]` · `[PORTAL: Tax Exemption Certificate Uploaded]`

### 2d. AfterShip tracking input columns `[LIVE — writable text]`

The portal reads these per order and passes **slug + tracking number** to AfterShip (see PLAN-1). Slug = AfterShip carrier code (e.g. `fedex`, `estes`). These are the current source for shipment tracking, replacing DS-03 (frame) and DS-11 (mats).

| Ref | Shipment | Field | Monday column ID |
|---|---|---|---|
| DS-29 | Sensory Gym Frame (SHP-1) | AfterShip slug (carrier) | `text_mm538vtm` |
| DS-30 | Sensory Gym Frame (SHP-1) | Tracking number | `text_mm53p3b2` |
| DS-31 | Therapy Mats & Padding (SHP-2) | AfterShip slug (carrier) | `text_mm51pap1` |
| DS-32 | Therapy Mats & Padding (SHP-2) | Tracking number | `text_mm51wdm5` |

**AfterShip create-tracking payload (per shipment):** required `tracking_number` (DS-30/DS-32) + `slug` (DS-29/DS-31); recommended `title` (order name), `order_id` (Monday order/subitem ID = match key for webhooks). API key stored in `.env` (`AFTERSHIP_API_KEY`), sent as the `as-api-key` header.

---

## 3. API endpoints (SYS-1)

| Ref | Endpoint | Method | Auth | Purpose |
|---|---|---|---|---|
| EP-1 | `/api/auth/send-code` | POST | public | Email a 6-digit login code (EM-01) |
| EP-2 | `/api/auth/verify-code` | POST | public | Verify code, issue customer session cookie |
| EP-3 | `/api/auth/session-check` | GET | cookie | Validate current customer session |
| EP-4 | `/api/auth/signout-customer` | POST | cookie | Clear customer session |
| EP-5 | `/api/auth/[...nextauth]` | — | Azure AD | Staff SSO (admin) |
| EP-6 | `/api/portal/setup` | POST | customer | Record a completed onboarding section → tagged update + flip checklist column (DS-24…28) |
| EP-7 | `/api/portal/invite` | POST | staff | Send portal invitation (EM-02) + log `[PORTAL: Invitation Sent]` |
| EP-8 | `/api/monday/order` | GET/PATCH | customer | Fetch the customer's order; PATCH updates address (DS-09) / logs contact change |
| EP-9 | `/api/monday/orders` | GET/PATCH | staff | Admin orders list; edit order fields |
| EP-10 | `/api/monday/columns` · `/debug-columns` | GET | staff | List board columns + IDs (use to fetch new column IDs) |
| EP-11 | `/api/monday/files` | GET/POST | mixed | List / attach files (DS-05) |
| EP-12 | `/api/monday/messages` | GET/POST | mixed | Read / post order messages (updates) |
| EP-13 | `/api/monday/boards` | GET | staff | List boards (settings) |
| EP-14 | `/api/monday/update-webhook` | POST | Monday | Fires on new Monday update; emails customer if a staff member replied (EM-11) |
| EP-15 | `/api/fedex/track?number=` | GET | mixed | Live FedEx tracking for a number (SYS-4) |
| EP-16 | `/api/jotform/webhook` | POST | secret | Jotform submission → match order → tag complete (EM-10 to team). **Extended 2026-07-15:** dispatches on `formConfig.tab` (`color` / `showcase` / default `documents`); the `showcase` branch scans `rawRequest` for URL-like strings (classified image vs. video by file extension, not by Jotform field key names) and routes to the UGC pipeline (DS-48…53) instead of the one-time checklist path — see PLAN-9. |
| EP-17 | `/api/cron/reminders` | GET | CRON_SECRET | Scheduled reminders (SYS-7) |
| EP-18 | `/api/admin/notify-installation` | POST | staff | Send "installation ready" email (EM-07) |
| EP-19 | `/api/settings/forms` | GET/POST | staff | Manage Jotform form mapping |
| EP-20 | `/api/aftership/track?slug=&number=` | GET | customer (ownership-checked) / staff | Live AfterShip status for a tracking number — Frame/Mats/accessory items (DS-29…32, DS-46/44) |
| EP-21 | `/api/aftership/webhook` | POST | AfterShip (`x-webhook-secret` custom header, HMAC fallback) | `[LIVE, CONFIRMED 2026-07-14]` Receives AfterShip tracking-update events; matches by slug+tracking# to an accessory subitem (DS-44) and writes the live status onto DS-45. Registered under AfterShip's **Notifications → Webhooks** page (NOT Developers → Webhooks, which only covers Returns/Protection/Warranty) at `https://admin.aftership.com/notifications/webhooks` — URL `https://portal.summitsensory.com/api/aftership/webhook` + custom header `x-webhook-secret` matching `AFTERSHIP_WEBHOOK_SECRET` in Vercel. Test webhook confirmed a successful signature/auth check end-to-end. |
| EP-22 | `/api/monday/accessory-webhook` | POST | Monday (query-param secret) | `[BLOCKED]` Built to push the instant DS-44/46 change — Monday's API rejects webhook registration on a subitems board ("Creating webhook on subitems board isn't allowed"), and the documented workaround (`change_subitem_column_value` on the parent board) errored server-side on retry. Code is in place and harmless if Monday ever fixes this; EP-23 is the working mechanism today. |
| EP-23 | `/api/cron/accessory-tracking-sync` | GET | `CRON_SECRET` (Vercel Cron) | `[LIVE]` Every 15 min (vercel.json), scans all accessory items with both DS-46 (Carrier Code) + DS-44 (Tracking ID) set, onboards each into AfterShip (idempotent) and writes the current status to DS-45 if changed. Requires a Vercel plan supporting sub-daily cron frequency — Hobby tier runs this only once/day. **Post-mortem (2026-07-14):** this file's original header comment wrote the cron schedule literally as `"*/15 * * * *"` inside a `/** ... */` block comment — the embedded `*/` terminated the comment early, causing a webpack syntax error on every build that included this file. Every accessories-feature commit after this file was added therefore failed to build in production, even though GitHub had the correct code and Vercel's Git integration was healthy — it looked exactly like a stuck/broken deploy pipeline. Fixed by converting the header to `//` line comments (no terminator ambiguity). Lesson: never embed a literal `*/` inside a JS block comment. |
| EP-24 | `/api/referral/submit` | POST | customer | `[LIVE]` Validates friend name/email, calls `createReferralItem()` → REF-1…9 on the Referrals board (§2a-0), emails team (EM-15). |
| EP-25 | `/api/portal/email-upload-link` | POST | customer | `[LIVE]` Emails the signed-in customer a direct link (`https://form.jotform.com/{DS-47}`) to the Photo & Video Showcase form (EM-17), so they can open it on their phone without re-logging into the portal — the near-term alternative to SMS (no SMS provider is integrated). |
| EP-26 | `/api/admin/notify-color-form` | POST | staff | `[LIVE, 2026-08-13]` Sends EM-05 (Color Selection Form Ready). Triggered from the Orders table's "✉️ Notify…" dropdown. |
| EP-27 | `/api/admin/notify-task` | POST | staff | `[LIVE, 2026-08-13]` Sends EM-06 (ad-hoc Action Required), with a free-text task description staff types at send time. Triggered from the Orders table's "✉️ Notify…" dropdown. |
| EP-28 | `/api/admin/impersonate` | POST | staff | `[LIVE, 2026-08-13]` Mints a short-lived (2hr) customer session for a given order, tagged with the staff member's email (`impersonatedBy`), so staff can view/act inside a customer's portal to help them complete a step or reproduce an issue. Logs `[PORTAL: Staff Viewing As Customer]` to the order; the portal shows a persistent banner with an "Exit to Admin" action whenever a session is impersonated. Triggered from the Orders table's "👁️ View as Customer" action. |

---

## 4. Email catalog (SYS-5 / Resend)

From: `portal@updates.summitsensory.com` · Team inbox: `orders@summitsensorygym.com`

| Ref | Email | Trigger | To |
|---|---|---|---|
| EM-01 | Login code | Customer requests sign-in (EP-1) | Customer |
| EM-02 | Portal invitation ("Action Required") | Staff clicks Invite (EP-7) | Customer |
| EM-03 | Setup reminder (escalating tone; urgent at #3+) | Cron (EP-17) until all sections done | Customer |
| EM-04 | Order status change (In Manufacturing / Ready to Ship / Shipped / Delivered) | Staff changes DS-02 **via the admin portal** (EP-9). ⚠️ Editing status **directly in Monday** does NOT trigger this — see OPEN-1 | Customer |
| EM-05 | Color selection form ready | `[LIVE, 2026-08-13]` Staff, via the Orders table "Notify…" dropdown (EP-26). Previously defined but unwired — see resolved OPEN-2 | Customer |
| EM-06 | Action required on a task | `[LIVE, 2026-08-13]` Staff, via the Orders table "Notify…" dropdown (EP-27), with a free-text task description | Customer |
| EM-07 | Installation materials ready | Staff (EP-18), now also reachable via the Orders table "Notify…" dropdown as of 2026-08-13 — previously the endpoint existed with no UI control at all | Customer |
| EM-08 | New file shared | File attached via admin portal (EP-11) | Customer |
| EM-09 | Balance / payment update | Staff changes balance **via the admin portal** (EP-9) | Customer |
| EM-10 | Form completed | Jotform webhook (EP-16) | Team |
| EM-11 | Team replied to message | Monday update webhook (EP-14) | Customer |
| EM-12 | Incoming customer message | Customer posts in Messages (EP-12) | Team |
| EM-13 | Contact info changed (⚠️ verify before shipment) | Customer edits contact/delivery | Team |
| EM-14 | Tax exemption certificate submitted | Customer uploads cert (EP-6 `tax_exemption`) | Team | `[LIVE — restored 2026-07-28]` |
| EM-15 | New referral submitted | Customer submits Refer a Friend form (EP-24) | Team | `[LIVE]` |
| EM-16 | Photo/video reward tier reached | `incrementUgcCounts()` detects a new tier crossed (EP-16 showcase branch) | Team | `[LIVE]` Prompts staff's 30-second quality check (angles, usage, video length) before manually approving the reward |
| EM-17 | Photo/video upload link | Customer clicks "Email Me This Link" on the Showcase tab (EP-25) | Customer | `[LIVE]` |

---

## 5. End-to-end process flow (proposal signed → delivered)

Each stage: **Trigger → Actor → System(s) → Data source(s) → Output/next.**

### Phase 1 — Order created & portal opened

**S-1.1 Proposal signed / order entered** `[LIVE, upstream]`
- **Trigger:** Customer signs proposal.
- **Actor:** Staff.
- **System:** SYS-2. An order item exists on the Manufacturing Process board (DS-01 email, DS-02 status = *Order Placed*, plus mirrored contact fields DS-18…22).
- **Note:** Freight carrier/tracking entry happens on a separate board today — see PLAN-2 for how that connects into this order and its subitems.

**S-1.2 Staff sends portal invitation** `[LIVE]`
- **Trigger:** Staff clicks **✉️ Invite** in the admin orders table (SCR-11).
- **System/endpoint:** EP-7 → sends EM-02 → writes `[PORTAL: Invitation Sent]` to DS/updates.
- **Output:** The reminder clock (SYS-7) starts from this timestamp.

**S-1.3 Customer logs in** `[LIVE]`
- **Trigger:** Customer clicks the portal link, enters email.
- **System/endpoint:** EP-1 (EM-01 code) → EP-2 (verify) → session cookie issued → lands on Dashboard (SCR-1).

### Phase 2 — Customer onboarding (the 5 setup sections)

The portal shows a 5-step setup checklist. Completing each one: (a) posts a tagged update (§2c), and (b) flips its Status column to `✅` (DS-24…28) via EP-6. Manufacturing should not begin until all 5 are complete.

**S-2.1 Contact Information** `[LIVE]` → EP-6 (`contact`) → tag `[PORTAL: Contact Confirmed]` → DS-24 ✅. Contact edits write via EP-8 (address DS-09 direct; phone/name as a change request + EM-13 to team). As of 2026-07-13, Primary Contact's Name/Email/Phone are sourced from DS-36/37/38 (distinct mirror columns from the Delivery POC mirrors) and are all required — the tab auto-opens its edit form if any are missing from Monday.

**S-2.2 Billing Information** `[LIVE]` → EP-6 (`billing`) → tag `[PORTAL: Billing Information]` → DS-25 ✅ → EM-13/team notify. As of 2026-07-13, the "same as delivery address" toggle was removed — the Bill-To Address form is always shown, pre-filled from DS-39 (address) and DS-40 (zip).

**S-2.3 Delivery & Site Details + Freight Acknowledgment** `[LIVE]` → EP-6 (`delivery` and `freight_ack`) → tags `[PORTAL: Delivery Details]` / `[PORTAL: Freight Delivery Acknowledgment]` → DS-26 ✅. Restricted changes (address, liftgate, dock, delivery timing) flag the team (EM-13). As of 2026-07-13 this step also collects: an optional Secondary Delivery POC (same fields as primary), with **each of Primary and Secondary POC having their own independent Preferred Communication Method + mobile number** (no longer a single shared communication-preferences card); a required loading dock / liftgate question (default "No, I need liftgate delivery"); delivery timing as ship-ASAP vs. schedule-on-or-after-date (replaces the old earliest/latest date-range window); and a confirm-on-file / expand-to-edit ship-to address flow supporting international addresses (Address Line 1/2, City, State/Province, ZIP/Postal, Country). The "on file" ship-to address default now sources from the **Billing Information tab's Bill-To Address** (DS-39/DS-40) rather than DS-09 (Confirmed Delivery Address) — everything else in Delivery Logistics is unchanged. Every submission is also logged as a new row on the standalone **Delivery & Site Details Submissions** board (see §2a-1) via `createDeliverySubmissionItem()`, in addition to the tagged update and DS-09/DS-26 writes on the order's Manufacturing Process row.

**S-2.4 Color & Product Selections** `[LIVE]` → Jotform embedded (form ID from DS-16, fallback DS-17). On submit, EP-16 matches the order by email and tags `[PORTAL: Color Selections]` → DS-27 ✅ + EM-10 to team. Customer may also be prompted by EM-05 when the form is assigned.

**S-2.5 Required Documents** `[LIVE]` → Jotform (documents forms) → EP-16 → tag `[PORTAL: Documents Submitted]` → DS-28 ✅ + EM-10.

**S-2.6 Reminders until complete** `[LIVE]` → SYS-7 (EP-17) runs weekdays 8:00 AM Mountain (`0 14 * * 1-5`). Every `REMINDER_INTERVAL_DAYS` (default 3), up to `REMINDER_MAX_COUNT` (default 6), it emails EM-03 listing only the incomplete sections, and stops automatically once all 5 tags are present. Each send logs `[PORTAL: Reminder #N]`.

### Phase 3 — Manufacturing

**S-3.1 Order moves into production** `[LIVE]`
- **Trigger:** Staff sets DS-02 (Manufacturing Phase) = *In Manufacturing*.
- **Effect:** Portal progress bar advances to stage 1 (SCR-2). EM-04 ("In Manufacturing") is sent **only if the status is changed through the admin portal (EP-9)**; changing DS-02 directly in Monday.com does not send it — see OPEN-1.
- **Recommended gate:** don't advance until DS-24…28 are all `✅` (see PLAN-3 automation).

**S-3.2 Ready to ship** `[LIVE]` → DS-02 = *Ready to Ship* → stage 2 → EM-04 ("Ready to Ship").

### Phase 4 — Shipping & tracking

**S-4.1 Tracking numbers entered** `[LIVE / evolving]`
- **Today:** frame tracking comes from DS-29/30 (AfterShip slug + tracking, direct columns); mats/padding from DS-31/32 (same pattern). Therapy Equipment & Accessories (SHP-3) tracking comes from **Monday subitems** — staff add one subitem per misc item with Order Status (DS-41), Carrier Code (DS-46 — the actual AfterShip slug), and Freight Tracking ID (DS-44) — see PLAN-2, PLAN-4.
- **Still planned:** migrating Frame and Mats (SHP-1/SHP-2) from direct board columns onto the same subitem pattern used for SHP-3 — see PLAN-2.

**S-4.2 Status = Shipped** `[LIVE]` → DS-02 = *Shipped* → stage 3 → EM-04 ("Shipped").

**S-4.3 Customer tracks shipments** `[LIVE]`
- **Screen:** Order Status & Tracking (SCR-2). Renders one card per shipment (SHP-1/2/3).
- **Data:** each card calls EP-15 (FedEx Track) per tracking number and shows status + last events.

**Shipment cards on SCR-2:**

| Ref | Card title | Current carrier label | Current data source | Planned |
|---|---|---|---|---|
| SHP-1 | Sensory Gym Frame | "FedEx Freight" | DS-03 → moving to **DS-29 (slug) + DS-30 (tracking)** | AfterShip via DS-29/DS-30 (PLAN-1) |
| SHP-2 | Therapy Mats & Padding | "Standard Carrier" (hidden if `N/A`) | DS-11 → moving to **DS-31 (slug) + DS-32 (tracking)** | AfterShip via DS-31/DS-32 (PLAN-1) |
| SHP-3 | Therapy Equipment & Accessories | per-item, from subitem's Carrier Code (DS-46) | Monday **subitems** on the Manufacturing Process board (DS-41…46) — replaces the old DS-12 free-text column | AfterShip via DS-46/44; onboarded by cron (EP-23) and kept current by webhook (EP-21) into DS-45 — see PLAN-2, PLAN-4 |

### Phase 5 — Delivery & post-delivery

**S-5.1 Delivered** `[LIVE]` → DS-02 = *Delivered* → stage 4 → EM-04 ("Delivered").

**S-5.2 Installation materials** `[LIVE]` → Staff triggers EP-18 → EM-07. Installation tab (SCR-7) shows videos/docs/links from DS-13/DS-14/DS-15.

**S-5.3 Ongoing support** `[LIVE]` → Messages (SCR-10, EP-12) two-way: customer message → EM-12 to team; staff reply in Monday → EP-14 → EM-11 to customer. Files (SCR-8) and Invoice/Payment (SCR-9, DS-06/DS-07/DS-23) remain available.

**S-5.4 Tax exemption certificate** `[LIVE]` → Invoice & Payment tab (SCR-9) → customer selects Yes/No. **No** → EP-6 (`tax_exemption`) sets DS-34 = *No* + tag `[PORTAL: Tax Exempt - No]`; nothing further requested. **Yes** → customer uploads a certificate → EP-6 uploads the file to DS-33 via the multipart `/v2/file` endpoint, sets DS-34 = *Yes*, tags `[PORTAL: Tax Exemption Certificate Uploaded]`, and sends EM-14 to the team for review. Portal always displays: without an approved certificate on file, sales tax applies to the invoice.

---

## 6. Customer portal screen map (`/portal`)

**Setup tabs (onboarding):** Contact · Billing · Delivery & Site Details · Color & Product Selections · Required Documents (map to S-2.1…2.5).

**Order tabs (ongoing):**

| Ref | Tab | Purpose | Key data sources |
|---|---|---|---|
| SCR-1 | Dashboard | Overview + setup progress | DS-02, DS-24…28 |
| SCR-2 | Order Status | Progress bar + shipment tracking | DS-02 (bar), SHP-1/2/3, EP-15 |
| SCR-7 | Installation | Videos, docs, resources | DS-13/14/15 |
| SCR-8 | Files & Documents | Shared files | DS-05, EP-11 |
| SCR-9 | Invoice & Payment | Invoice link, balance, tax exemption cert upload | DS-06, DS-07, DS-23, DS-33, DS-34 |
| SCR-10 | Messages | Two-way messaging | EP-12, EM-11/EM-12 |
| SCR-12 | Refer a Friend | `[LIVE]` Native form (name/email/phone/message) → EP-24 → REF board; positioned in the lower half of the "My Order" nav group | EP-24, §2a-0 |
| SCR-13 | Photo & Video Showcase | `[FULLY LIVE 2026-07-28 — see PLAN-9]` Jotform embed (form `261963488177069`, from DS-47/`JOTFORM_SHOWCASE_FORM_ID`) + "Email Me This Link" button (EP-25); marketing copy covers the $25/10-credit reward and video requirements (≥20s, different angles, people using the frame) | EP-16 (showcase branch), EP-25, DS-47…53 |
| SCR-11 | Contact Us | Support info | static |

**Admin portal (`/admin`, staff):** orders table with inline edit + **✉️ Invite** (EP-7/EP-9) + setup-progress dots (DS-39), customers list (also with progress dots), file manager (EP-11), messages (EP-12) — sorted with Needs-Reply threads first and a nav badge count, driven by the Message Status queue column (DS-40) — and settings for column mapping (EP-10), Jotform forms (EP-19), and boards (EP-13). See PLAN-10.

---

## 7. Planned / open items (change backlog)

| Ref | Item | Status | Summary |
|---|---|---|---|
| PLAN-1 | **AfterShip tracking engine** | `[PLANNED]` | Add `lib/aftership.js` (carrier-agnostic); make the track endpoint carrier-aware; retire FedEx-only limitation. Carrier "slug" examples: FedEx=`fedex`, Estes=`estes`, UPS=`ups`, USPS=`usps`. |
| PLAN-2 | **Shipments as Monday subitems** | `[LIVE for Therapy Equipment & Accessories only]` | Built 2026-07-14 for the 3rd shipment section (DS-41…45, SHP-3). Each accessory item = a subitem with Order Status (Order Pending/Ordered/Out of Stock), Date Ordered, Freight Carrier (=AfterShip slug), Freight Tracking ID, and Carrier Status (auto-synced live by the AfterShip webhook, EP-21). Frame (SHP-1) and Mats (SHP-2) remain on their existing direct board columns (DS-29…32) — **not yet** migrated to subitems. |
| PLAN-3 | **Freight board → Manufacturing subitem sync** | `[PLANNED]` | Carrier + tracking entered on a separate board; match to this order (prefer a shared Order ID over customer name) and write to the correct subitem (e.g. Therapy Mats & Padding) via Make.com or integration code. |
| PLAN-4 | **"Therapy Equipment & Accessories" section** | `[LIVE]` | Built 2026-07-14. Portal card sourced from Monday subitems (SHP-3, DS-41…46) instead of the old DS-12 free-text column. Three display tiers: Order Pending → Ordered (no tracking yet) → full live AfterShip tracking once Carrier Code + Tracking ID are set on the subitem, matching Frame/Mats fidelity. "Out of Stock" gets its own message. Freight status reaches Monday via a cron sync (EP-23) + AfterShip webhook (EP-21), not a Monday-side webhook — Monday's API doesn't allow webhooks on subitems boards (see EP-22). |
| PLAN-5 | **Notifications via portal's own layer** | `[PLANNED]` | Route AfterShip status webhooks → Resend (SYS-5) so wording/branding is controlled and carrier/Amazon references can be stripped. |
| PLAN-6 | **"Ready to Manufacture" gate & view** | `[PLANNED]` | Monday view filtered to DS-24…28 all `✅`; optional automation to auto-advance DS-02. |
| PLAN-7 | **Auto-send portal invite on "Send Invite" status** | `[FULLY LIVE 2026-08-06]` | When a Manufacturing item's invite-status column (`color_mm5427cr`, "Customer Portal Invite") is set to **"Send Invite"**, `pages/api/monday/invite-webhook.js` fires the invite (EM-02 + `[PORTAL: Invitation Sent]`) and flips the column to "Invite Sent". Fixed 2026-07-27: the webhook called `setStatusLabel()`/referenced `COLS.inviteStatus`, neither of which existed in `lib/monday.js` — both now added. **2026-08-06:** found that a matching automation already existed on the board (created 2026-07-12, recipe "When a status changes to something, send a webhook") but its destination was a stale connection, not this endpoint — Bryan re-pointed it via Monday's UI, confirmed reaching the endpoint via Vercel logs. Also **removed the idempotency skip** that blocked re-sending: previously any order with a prior `[PORTAL: Invitation Sent]` tag silently ignored further triggers, which blocked intentional resends (a customer losing the email, staff wanting to test); since setting the status to "Send Invite" is itself a deliberate action, not something that loops automatically, every trigger now sends — first time or resend — with the logged Monday update worded "sent" vs "re-sent" accordingly. No remaining manual steps. |
| PLAN-8 | **Tax Exemption Certificate upload (SCR-9)** | `[LIVE — restored 2026-07-28, originally built 2026-07-11]` | "Tax Exemption Certificate" module on the Invoice & Payment tab (`TaxExemptionCard`). Yes/No selector writes DS-34; choosing "Yes" requires uploading a certificate, which writes the file to DS-33 via `uploadFileToColumn()` — a real multipart request to Monday's dedicated `/v2/file` endpoint (`add_file_to_column` needs `file: File!`, not a URL string, so the URL-based helpers used elsewhere in this codebase — `addFileToOrder`, `attachUgcFile` — don't apply here). Root-cause history: this was built and working 2026-07-11 (confirmed by real customer-tagged Monday updates dated as late as 2026-07-13 — `[PORTAL: Tax Exempt - No]`, `[PORTAL: Tax Exemption Certificate Uploaded]`), then silently deleted from all three files (`lib/monday.js`, `pages/api/portal/setup.js`, `pages/portal/index.js`) in commit `4b9da3e` (2026-07-13 14:28) — an apparent accidental clobber during an unrelated large refactor, not an intentional removal (the same commit's diff shows no comment or intent to remove it). It sat undetected for two weeks since nothing in the app errors when a feature is just *missing* — Bryan noticed only because he went looking for where to enter tax-exempt status. Recovered verbatim from git history (`git show 45d47db:<file>`, the commit just before the deletion) rather than rebuilt from scratch, so behavior exactly matches what was live in July. **Lesson:** when a "was this ever built?" question comes up, check git history (`git log -S"<term>"`) before assuming it was only ever planned — the docs said `[PLANNED]` for two weeks purely because whoever/whatever caused the July 13 regression also reverted the doc's own PLAN-7/PLAN-8 entries to their pre-build wording, which is itself a symptom worth watching for. |
| PLAN-10 | **Staff setup-progress dashboard + assisted-entry mode** | `[BOTH PARTS LIVE — dashboard 2026-07-27, assisted-entry 2026-08-13]` | Bryan's concern: customers who struggle with the portal call in for help, and staff had no quick way to see what's incomplete. Two-part design: (1) **Progress dashboard** — read-only 5-dot readout (DS-39) on the Admin Orders and Customers tabs showing Contact/Billing/Delivery/Colors/Documents status per order (`ProgressDots` component). Shipped 2026-07-27, no new auth surface. (2) **Assisted-entry mode ("View as Customer")** — shipped 2026-08-13, EP-28. Mints a short-lived (2hr) customer session tagged with the staff member's email; drops staff into the real portal UI scoped to that order with a persistent "Viewing as staff" banner (not a "Staff Assisted Session" banner as originally sketched — same purpose) and an "Exit to Admin" action; logs `[PORTAL: Staff Viewing As Customer]` to the order. Staff can literally fill in fields for the customer over the phone, or reproduce/diagnose exactly what's broken for them. |
| PLAN-11 | **Email Template Management System** | `[DESIGN COMPLETE 2026-08-13; foundation code shipped, DB not yet provisioned]` | Nontechnical-staff-editable email templates (create/edit/preview/test/version/publish) for all 17 EM-xx emails, without code deploys. Full architecture in `Email-Template-System-Design.md` (project root). Requires a new, narrowly-scoped Postgres database (Vercel Postgres — chosen by Bryan 2026-08-13, **not yet provisioned**: create one from Vercel dashboard → Storage → Create Database, link to this project) — Monday remains the system of record for orders/customers, untouched. Foundation code shipped and syntax-verified: `prisma/schema.prisma` (EmailTemplate/Version/Component/TestSend/AuditLog/StaffPermission), `lib/email-templates/{triggers,render,blocks,sanitize,db}.js`, `lib/permissions.js`. **Not yet done:** `lib/email.js`'s 17 functions still send their existing hardcoded HTML directly (not yet cut over to `renderEmail()`) — that happens template-by-template once the database exists, per the migration plan, each verified with a real test send before its hardcoded fallback is removed. The admin-facing Template Library/Editor UI (browsing, drafting, publishing, versioning, test-send, the permissions grid) has not been built yet — that's the next chunk of work once the DB is live. |
| PLAN-12 | **Portal-wide data-persistence & confirmation-feedback audit (Kalen Siddens complaint)** | `[6 root causes found and fixed 2026-08-17]` | Triggered by a real customer call (Kalen Siddens, order `12789699126`, "odd errors and not seeing certain things"). Pulled his actual Monday record to ground the investigation instead of guessing. Found and fixed six distinct bugs: **(1)** the portal only ever read setup-completion state (`completions`) from `localStorage`, never from the durable Portal:Contact/Billing/Delivery/Colors/Documents status columns Monday already tracks (`order.progress`, computed by `parseOrderItem` all along but never consumed) — so a different browser, cleared cookies, or staff opening the portal on another machine made a customer's already-completed steps look wiped. `loadOrder()`/`OrderPicker` now merge `order.progress` in as the source of truth, with localStorage only a same-browser cache. **(2)** Color and Required Documents tabs' "Mark as Complete" buttons never called `/api/portal/setup` at all — purely local/localStorage state — so Monday's Portal:Color/Portal:Required columns never flipped no matter how many times a customer clicked it; wired both to the existing (already-built, just never called) `saveSetup('color'|'documents', {})` backend cases. **(3)** the Delivery tab's ~20 fields (secondary POC, comm preferences, loading dock, delivery timing, ship-to address components, freight-ack name) were written only to a free-text tagged update and the separate Delivery & Site Details Submissions board (`18421779422`) — never back onto the order item — so the form reset to blank every time the tab unmounted, even right after a successful submission. Same problem for Billing's decomposed address fields + billing POC (only the combined address string persisted). Fixed by adding two new additive Monday columns on the Manufacturing Process board — `long_text_mm6a5z48` ("Portal: Delivery Answers (JSON)") and `long_text_mm6ancsh` ("Portal: Billing Answers (JSON)"), `COLS.deliverySnapshot`/`COLS.billingSnapshot` — written on every submit and parsed back by `parseOrderItem` as `order.deliverySnapshot`/`order.billingSnapshot`, which `DeliveryTab`/`BillingTab` now seed their initial state from. These writes are NOT best-effort (unlike the submissions-board write, which stays best-effort/supplementary) — a failure now surfaces as a real save error instead of a false "saved" confirmation. **(4)** `pages/api/cron/reminders.js` determined "incomplete" tabs by scanning update history for one exact legacy tag per tab (e.g. `[PORTAL: Contact Confirmed]`) — a customer who used the Contact tab's "edit and submit changes" path instead posts `[PORTAL: Contact Update Requested]`, which never matched, so the cron disagreed with Monday's own status column. Confirmed on Kalen's order: Portal:Contact showed ✅ since 2026-08-13, but he got a reminder on 2026-08-17 claiming Contact Information was still incomplete. Now reads `order.progress` directly — the same source of truth used by fix (1) — so it can't drift out of sync again. **(5)** Monday's `create_update` API always attributes a message to whoever owns `MONDAY_API_TOKEN`, never to the actual customer — confirmed literally every message Kalen sent through his portal Messages tab (including "Wouldn't let me change cage color") showed creator "Bryan Shepherd." The portal's `isStaff()` check relied on `creator.email`, so a customer's own message rendered as if it came from staff, and the unread-reply badge (which, separately, had its filter polarity backwards) was permanently stuck at 0. Fixed by having `/api/monday/messages` stamp the true origin into the body itself (`[PORTAL:CUSTOMER]`/`[PORTAL:STAFF]`) and having the frontend's new `messageIsStaff()` read that tag (falling back to the old email guess only for pre-fix message history), plus correcting the unread-badge filter to count staff messages instead of the customer's own. **(6)** the Contact tab's "Submit Changes" flow (for corrections, since Name/Phone/Email are read-only Monday mirrors by design) gave a toast and then reverted to showing the OLD value with zero indication anything was pending — indistinguishable from the correction having silently failed. Now persists the pending correction locally and shows it (with a "pending review" note) until Monday's mirror actually catches up to match, then auto-clears. **Not fixed / flagged only:** Kalen's literal "wouldn't let me change cage color" symptom during the Color Selection Jotform embed itself couldn't be reproduced or root-caused from code alone — plausibly the third-party Jotform iframe, or the documented `dangerouslySetInnerHTML`/React-reconciler risk already called out in `ColorTab`'s comments if he switched tabs mid-form. Investigate live if it recurs. All six code fixes verified with `esbuild` and delivered to the live git-clone repo; the two new Monday columns are additive only (no existing data touched). |
| PLAN-13 | **Second codebase-wide audit: 11 additional bugs found and fixed** | `[11/11 fixed 2026-08-17]` | Follow-up to PLAN-12 — Bryan asked whether issues existed beyond the Kalen Siddens complaint. A broader audit (admin dashboard, auth, shipment tracking/webhooks, referrals, Jotform routing, invites) surfaced 11 more real bugs, fixed in the order presented, each verified against the actual code (not assumed). **(1)** `pages/api/monday/accessory-webhook.js`'s secret check failed OPEN (`if (secret && ...)`) — with `MONDAY_ACCESSORY_WEBHOOK_SECRET` unset in Vercel, anyone could POST fake Carrier Code/Tracking ID change events. Fixed to fail closed (`if (!secret || ...)`), mirroring the AfterShip webhook's `isAuthorized()` pattern. **⚠️ Requires `MONDAY_ACCESSORY_WEBHOOK_SECRET` to already be set in Vercel AND the registered Monday webhook URL to already include `?secret=<value>` — otherwise this endpoint now rejects every real Monday event too. Verify both are configured (Vercel env vars were not independently confirmable from this session).** **(2)** `attachUgcFile()` sent the Jotform file URL as a JSON `$file: String!` GraphQL variable — Monday's `add_file_to_column` requires `file: File!` via a real multipart upload to `/v2/file`, so every "Photo & Video Showcase" submission silently failed to attach anything while the webhook still logged success and credited the reward tally. Fixed to download the file and upload via the existing `uploadFileToColumn()` multipart helper (same mechanism used by the Tax Exemption Certificate upload, PLAN-8); the Jotform webhook call site was also fixed to only count/log/credit files that actually attached, and to return a real error if every attach in a submission failed. **(3)** `addFileToOrder()` (the admin "upload a file to an order" endpoint) had the identical JSON-vs-multipart bug — same fix; its call site already had correct error handling. **(4)** `getOrderByEmail()`/`getOrdersByEmail()` didn't sort by recency, so a repeat customer with more than one order could land on an arbitrary one — not necessarily their newest — at portal login or Jotform-submission routing. Now sorted most-recent-first by `createdAt`. **(5)** Several Monday lookups (`getAllOrders`, `findAccessorySubitemByTracking`, `findOrderByFreightTracking`, `getAllAccessoryItems`) used a single hard-coded `items_page(limit: 500)` (or, for `getAllOrders`, a caller-supplied limit as low as 100 for the admin dashboard, 200 for the reminders cron) with no pagination — any board growing past that count would silently drop items from admin views, cron jobs, and freight/accessory tracking matches. Added a shared `fetchAllBoardItems()` helper that pages through Monday's cursor-based `items_page`/`next_items_page` in full; all four functions (plus the reminders and accessory-tracking-sync crons' call sites) now see the complete board regardless of size. **(6)** the AfterShip webhook sent the customer freight-update email and wrote the per-shipment dedupe tag inside one try/catch — if the email sent but the tag write failed, the next matching checkpoint would re-send a duplicate email with no record the first had gone out. Split into two steps: an email-send failure now skips the tag write entirely (so the next attempt retries the send), while a tag-write failure after a successful send is logged loudly with the exact manual Monday fix needed, instead of being silently swallowed by the outer catch. **(7)** `pages/api/portal/invite.js` had the same non-atomic pattern — a Monday log-write failure after a successful invite email reported "Failed to send invitation" to staff, who would then resend and duplicate the customer email. Now returns a real success (with a warning) when only the log write fails, and a real failure only when the email itself didn't send. **(8)** the AfterShip webhook assumed payload shape `payload.msg` (with an unverified `payload.data.tracking` fallback) with no logging if neither matched. Now logs which shape was actually used, and warns loudly on a genuinely unmatched shape, so a real AfterShip payload-format change surfaces in logs instead of silently no-op'ing forever. **(9)** the Jotform webhook's `extractEmail()` only checked top-level string values or one level of `{answer: ...}` nesting, with weak `@`+`.` validation that could false-match unrelated text fields. Rewritten to recurse through nested objects/arrays the same way `extractShowcaseFiles()` already does, using a real email-pattern regex. **(10)** a Required Documents (or Color Selections) tab backed by more than one Jotform form flipped Portal:Documents/Portal:Colors to ✅ the instant ANY one of its forms arrived, not all of them. Now embeds the form ID in the completion tag and checks Monday's update history for every other form mapped to that tab (via the already-documented `JOTFORM_FORM_MAP` `tab` field) before marking the section complete. **(11)** `notifyTeamNewReferral`/`notifyTeamUgcThreshold`'s "View in Monday" buttons both linked to the generic `/admin` dashboard instead of the actual Referrals-board item or order. Now build direct Monday pulse links (`https://{MONDAY_ACCOUNT_SLUG}.monday.com/boards/{boardId}/pulses/{itemId}`, the same pattern already used in `createReferralItem()`), falling back to `/admin` only if an item ID genuinely isn't available. All 11 fixes verified with `esbuild` and delivered to the live git-clone repo; no new Monday columns required. |
| PLAN-14 | **Deep technical audit (19-phase, user-directed) → full remediation of 27 of 29 findings** | `[27/29 REMEDIATED 2026-08-18; 2 left as open decisions]` | Bryan requested an explicit, skeptical, no-code-changes-until-reviewed audit (see `Summit-Portal-Deep-Audit.docx` and the Claude Project doc `deep-technical-audit-2026-08-18.md` for the full 29-finding register: 6 Critical, 12 High, 8 Medium, 1 Low, 2 Improvement; Readiness Score 38/100). Bryan then directed full remediation of every finding. **All 6 CRITICAL fixed:** (001) `lib/auth.js` no longer falls back to a hardcoded JWT secret — throws at startup if `NEXTAUTH_SECRET` is unset. (002) Stored XSS in the Messages thread (`pages/portal/index.js`, `pages/admin/index.js`) — `dangerouslySetInnerHTML` now sanitized via a new `lib/sanitizeHtml.js` (DOMPurify, browser-only, small allowed-tag set, forces safe `target`/`rel` on links). (003) `pages/api/monday/update-webhook.js` now requires a shared secret (`MONDAY_UPDATE_WEBHOOK_SECRET`, fails closed), mirroring the accessory-webhook pattern. (004) `parseOrderItem()` now parses and returns `order.balance` (was always `undefined`); admin UI distinguishes "no balance data" (—) from a real $0/paid; the PATCH no-op-notify guard in `orders.js` now compares numerically. (005) `next` bumped 14.2.3→14.2.35, `next-auth` 4.24.7→4.24.15 (both CRITICAL CVEs patched; 0 critical/0 low-severity-below-moderate remain per a real `pnpm audit` — 36 vulnerabilities remain, all requiring a Next.js 15 major-version migration to fully clear, deliberately NOT attempted this session — see below). (006) SSRF/unbounded-download risk in `addFileToOrder()`/`attachUgcFile()` closed via a new `safeFetchUploadedFile()` helper in `lib/monday.js`: https-only, host-allowlisted (`UPLOAD_HOST_ALLOWLIST`, defaults to `jotform.com,jotfor.ms`), 15s timeout, 25MB streamed size cap (`UPLOAD_MAX_BYTES`). **21 of 23 HIGH/MEDIUM/LOW fixed:** (007) multi-order customers are no longer silently pinned to their newest order — `verify-code.js` leaves the session unbound when >1 order matches an email, and a new `pages/api/auth/select-order.js` endpoint re-binds the session once the customer picks from `OrderPicker`, so every other order-scoped endpoint's existing `session.orderId` authorization logic needed zero changes. (008) login code now uses `crypto.randomInt()` instead of `Math.random()`, plus a 5-attempt lockout tracked in the signed code-cookie itself (`MAX_CODE_ATTEMPTS`). (009) `pnpm-lock.yaml` regenerated and confirmed in sync via a real `pnpm install --frozen-lockfile`. (010) `pages/api/portal/setup.js` now validates required fields/email format server-side for every tab (`validateSetupData()`), mirroring `referral/submit.js`'s existing pattern. (011)/(012) Jotform and Monday invite webhook secret checks now fail closed instead of open when their env var is unset; invite-webhook no longer falls back to reusing `CRON_SECRET`. (013) added best-effort idempotency: Jotform webhook checks for a `(submission:ID)` marker in prior tagged updates before reprocessing; `referral/submit.js` checks for a same-friend/same-order referral created in the last 2 minutes (`findRecentReferral()`) before creating a duplicate. (014) `markSectionComplete()` failures are no longer silently swallowed at any of its 8 call sites — a new `markSectionCompleteSafe()` retries once and, on continued failure, the response now includes `checklistSyncPending: true` instead of an unconditional `{ok:true}`. (015) `pages/api/monday/order.js`'s GET handler and write-path order lookup are now wrapped in try/catch like every other route. (016) `createReferralItem()` now tracks which of its ~9 column writes failed and posts a visible "PORTAL: Referral Data Incomplete" flag naming them, instead of silently leaving a partially-written record indistinguishable from blank fields. (017) `setup.js`'s `delivery`/`freight_ack` cases now reject with 409 once `order.stageIndex` has reached "shipped," mirroring the frontend's existing UI gate. (018) the Delivery tab's two sequential `saveSetup('delivery', ...)` + `saveSetup('freight_ack', ...)` calls are now one atomic request — the freight acknowledgment is recorded inside the `delivery` case itself (same required fields were already being sent), eliminating the partial-failure/duplicate-retry window; the old `freight_ack` case is kept only for backward compatibility. (019) unknown emails at `send-code.js` now still get a signed (dummy) code cookie set, so `verify-code.js`'s response is identical to a real account with a wrong guess — closes the one-step-later enumeration leak. (020) `aftership/debug.js`'s `base` override is now validated against an allowlist (`api.aftership.com` + the configured `AFTERSHIP_API_BASE` host) instead of accepting any value. (021) added explicit timeouts (and a same-request-only retry for reads, never for mutations) to every outbound `fetch()` in `lib/monday.js`, `lib/aftership.js`, and a race-based timeout around the Resend SDK call in `lib/email.js`. (022) both cron jobs (`reminders.js`, `accessory-tracking-sync.js`) now `console.log` an explicit run summary (checked/reminded/skipped/errors or checked/updated) so a partial run is visible in Vercel's logs, not just in an HTTP response body nobody reads. (024) `colorFormId`/`showcaseFormId` are now validated as numeric Jotform IDs (`isValidJotformId()`) before being interpolated into the embed's `dangerouslySetInnerHTML` template. (026) deleted the three confirmed-orphaned root files (`aftership.js`, `debug.js`, `probe.txt` — none were imported anywhere). (027) added a lightweight in-memory per-IP/per-session rate limiter (`lib/rateLimit.js`) to `send-code.js` (5/min/IP), `referral/submit.js` (10/min/session), and `setup.js` (20/min/session) — explicitly documented as single-instance/cold-start-scoped, not a durable cross-instance limit; upgrade to Vercel KV/Upstash if abuse becomes real. **2 initially left as open decisions, now resolved — see PLAN-15:** (023) structured error monitoring and (025) the dormant Email Template Management System were both left for Bryan's decision at the time this row was written; Bryan then directed Claude to decide and implement both, which PLAN-15 documents. **2 deliberately deferred as routine maintenance, not remediation (028/029):** `eslint`/`uuid` version bumps and a NextAuth v5 migration — both are major-version jumps with real breaking-change risk and no test suite to catch regressions; scheduling these is a planning decision, not something to force through opportunistically. **Verification:** every changed file passed an `esbuild` syntax check individually; a full real `pnpm install` + `next build` (production mode, all 39 routes including the new `/api/auth/select-order`) compiled and generated successfully end-to-end after all changes, not just per-file. **Known residual gaps, stated plainly:** no live two-customer cross-account test was run against a real Monday account (static analysis only, same caveat as the original audit); 36 dependency vulnerabilities remain (0 critical, ~20 high/14 moderate/2 low), effectively all requiring a Next.js 15 migration to fully clear; the in-memory rate limiter and Jotform/referral idempotency checks are best-effort within a single serverless instance, not a durable distributed guarantee. |
| PLAN-15 | **PORTAL-023 (monitoring) + PORTAL-025 (dead Email Template System) resolved by Claude's own decision, per Bryan's direction** | `[BOTH RESOLVED 2026-08-18]` | Following PLAN-14, Bryan explicitly directed: "you need to make the decision how to best resolve the outstanding issues and then create those fixes" — overriding the earlier stance of leaving these two for his decision. **(023) Internal failure alerting, no new account required:** rather than wiring up Sentry/Datadog with placeholder credentials (an account only Bryan can create), built a small internal alert path using infrastructure already live in the app. New `lib/monitoring.js` exports `reportCriticalFailure(source, message, details)`, which always logs to console first, then calls a new `sendInternalAlert()` in `lib/email.js` (Resend, defaults to `NOTIFY_TEAM_EMAIL`, overridable via new `ALERT_EMAIL` env var) to email a human. Deliberately wired into only the small set of failure points already flagged elsewhere as needing to "log loudly" — not routine security rejections, to avoid alert fatigue: `markSectionCompleteSafe()` in `lib/monday.js` (a checklist column silently out of sync after a retry fails, PORTAL-014's residual risk) and the top-level catch blocks of both cron jobs (`pages/api/cron/reminders.js`, `pages/api/cron/accessory-tracking-sync.js` — a run that doesn't complete, PORTAL-021/022's residual risk). If real APM is set up later, `lib/monitoring.js` is the one place to redirect from. **(025) Email Template Management System — archived, not deleted:** confirmed via repo-wide grep that nothing in `pages/**` (or anywhere else) references `prisma/schema.prisma`, any of `lib/email-templates/*.js`, or `lib/permissions.js`, and confirmed no build script forces `prisma generate`. Rather than deleting 682 lines of genuine unfinished design work (per PLAN-11) or leaving it live and unreachable (the original finding), moved all 7 files verbatim to `_archive/email-template-system/` with a README explaining what it is, why it was archived, exactly what changed in the active app, and the concrete steps to resume the work later. Removed the now-unused `prisma`/`@prisma/client` dependencies from `package.json` and regenerated `pnpm-lock.yaml`; removed the dead `DATABASE_URL` placeholder from `.env.example`. **Verification:** every touched/new file passed `esbuild`; ran a full real `pnpm install` (confirming `prisma`/`@prisma/client` are gone) and a clean `next build` (all 39 routes, zero errors) after both changes together, not just individually. **Result:** all 29 of the original audit's findings are now addressed in some form — 27 remediated in PLAN-14, 2 remediated here, 2 (028/029) deliberately deferred as routine maintenance, unchanged from PLAN-14's reasoning. |
| PLAN-16 | **Message sender identity fixed in Admin Messages tab (Bryan-reported, screenshot)** | `[FIXED 2026-08-18]` | Bryan reported (with an annotated screenshot of the Admin Messages tab) a customer's own message showing up under staff member "Bryan Shepherd" instead of the customer's name, and separately asked that staff-sent messages display as "Summit Sensory Gym," never an individual's name. Root cause: PLAN-12's fix (5) had already solved this correctly for the *customer-facing* portal (`pages/portal/index.js`'s `messageIsStaff()`, reading the `[PORTAL:STAFF]`/`[PORTAL:CUSTOMER]` tag `/api/monday/messages.js` stamps into every message body, since Monday's `create_update` API always attributes the update to whoever owns `MONDAY_API_TOKEN` — never the real sender) — but the **Admin Messages tab never adopted that fix**. It read `msg.creator?.email`/`msg.creator?.name` directly, which is exactly the field that's always wrong, and never stripped the tag from the visible body either (hence the raw "[PORTAL]" text visible in Bryan's screenshot). Fixed by extracting the logic into a new shared `lib/messageOrigin.js` — `isStaffMessage()`, `isStaffReply()`, `stripPortalTags()`, and a `messageDisplayName(staff, order)` helper — so the two views can't drift apart again; both `pages/portal/index.js` and `pages/admin/index.js` now import from there instead of each guessing independently. Per Bryan's explicit request, staff messages now always display as **"Summit Sensory Gym"** (`STAFF_DISPLAY_NAME`) in both views — never the individual staff member's name, even in the rare case a direct Monday reply genuinely does carry a real staff member's name in `creator.name`. Customer messages in the Admin view now display the actual customer/company name (`order.pocName || order.firstName || order.name || order.customerEmail`) instead of the wrong staff name. Untagged legacy messages (sent before PLAN-12's tagging existed) still fall back to the old creator-email domain guess, same as before — there's no way to retroactively know their true origin. **Verification:** `esbuild` on all 3 touched/new files, plus a full `next build` (all 39 routes, zero errors). |
| PLAN-17 | **Multi-order navigation: customers can now switch between orders without signing out** | `[LIVE 2026-08-18]` | Bryan: a customer with three separate orders (each with its own color selections, invoice, etc.) needed a way to move between them "without getting confused." PORTAL-007 (PLAN-14) had already solved this for the *first* order pick at login (`OrderPicker` + `/api/auth/select-order`, which re-binds the session cookie so every order-scoped endpoint's existing `session.orderId` authorization check needs no changes) — but once bound, `GET /api/monday/order` only ever returned that single order; there was no way to discover or reach the customer's *other* orders again short of signing out and back in (which re-triggers the multi-order branch in `verify-code.js`). Added: (1) `pages/api/monday/order.js` GET now supports `?all=1`, which — regardless of whether an order is already bound — looks up every order for the customer's email and returns `{ orders, currentOrderId }`; the default (no query param) response shape is completely unchanged, so no existing caller is affected. (2) `pages/portal/index.js` fetches that full list once in the background after an order is active (`allOrders` state) and reuses the list already in hand if the customer just came through the initial `OrderPicker`, so no redundant call in that path. (3) The top bar's order name is now a clickable switcher (only rendered when `allOrders.length > 1` — single-order customers see the exact same plain text as before, no new affordance for a choice they don't have) opening a new `OrderSwitcherModal`, styled to match the existing `OrderPicker` deliberately (a customer who already knows that screen shouldn't have to learn a second visual language), listing every order with a "Currently viewing" badge on the active one. Selecting a different order calls the existing `/api/auth/select-order` endpoint (re-binding the session, so no authorization logic anywhere else needed to change), then resets to the Dashboard tab so the customer lands somewhere orienting rather than a possibly-stale tab left over from the previous order. Per-order local setup-completion caching (`summit_setup_${orderId}` in localStorage) and the files/messages effects were already keyed off `order.id`, so switching correctly reloads that order's own data with no cross-contamination — confirmed by reading `applyCompletions`/`loadFiles`/`loadMessages`, not assumed. **Verification:** `esbuild` on both touched files, plus a full `next build` (all 39 routes, zero errors). **Not built:** a persistent "all my orders" overview/dashboard page — the switcher covers the stated need (navigate without confusion); a richer multi-order landing page is a bigger design decision Bryan should weigh in on if the 3-order customer (or others like them) wants more than a quick-switch. |
| OPEN-1 | Status/balance emails only fire from the admin portal | `[GAP]` | EM-04 and EM-09 are triggered by EP-9 (admin order edit) only. If staff change **Manufacturing Phase (DS-02)** or balance **directly in Monday.com**, no customer email is sent. Fix: add a Monday automation → webhook that fires EM-04 on DS-02 change regardless of where it's edited. |
| OPEN-2 | EM-05 / EM-06 defined but not wired | `[RESOLVED 2026-08-13]` | Both wired as manual staff triggers from the Orders table's "✉️ Notify…" dropdown (EP-26/EP-27) — see EM-05/EM-06 above. EM-07's endpoint (`notify-installation.js`) had the identical problem (a live endpoint with no UI control reaching it) and was folded into the same dropdown while fixing this. |
| OPEN-3 | Message Status doesn't flip to Replied when staff reply directly in Monday.com | `[RESOLVED 2026-07-28 — automation registered by Bryan, confirmed live via Vercel logs]` | Bryan reported replying to a customer inside Monday.com left Message Status (DS-40) stuck on "Needs Reply." Two separate causes found: (1) the **Admin Portal reply path** had a real code bug — `pages/api/monday/messages.js` only ever set Message Status on the *customer* POST branch; a staff reply from the Admin Portal Messages tab never cleared the flag. **Fixed 2026-07-28** — a staff branch now sets it to Replied. (2) The **raw Monday Updates reply path** (replying directly on the item in Monday.com, not through the Admin Portal) depends entirely on `update-webhook.js` (EP-14), which only fires if a Monday automation "When an update is created → send a webhook → `https://portal.summitsensory.com/api/monday/update-webhook`" is registered on this board. Vercel production logs show **zero requests** to that endpoint in the trailing 24 hours, confirming the automation does not currently exist (three unrelated "Webhooks" app automations already on the board, created Nov 2025–Feb 2026, are a different integration — most likely the AfterShip/Zapier accessory-tracking push from PLAN-2/EP-22 — and are not this trigger). Attempted to register it via the monday.com MCP connector's automation-builder: it correctly resolved the trigger ("When update created") but the outgoing-webhook action requires selecting a pre-authorized destination through Monday's own Webhooks-app UI — it won't accept a freeform URL via the API, the same limitation already documented for PLAN-7. **Registered 2026-07-28:** Bryan added the automation via the board's Automations → Sync → Webhooks templates → "When a new update posted, send a webhook" card (Monday's actual UI label for this trigger). The pre-authorized-destination requirement (the API limitation described above) meant the connection had to be added fresh via "Use another account" in the destination picker, since the existing saved connections were all unrelated `hooks.zapier.com` URLs from the AfterShip integration — entered `https://portal.summitsensory.com/api/monday/update-webhook` there instead. Confirmed live via Vercel: `get_runtime_logs` grouped by `requestPath` showed 2 requests to `/api/monday/update-webhook` within 2 hours of activation (previously zero). Replying to a customer directly in Monday's Updates feed now flips Message Status to Replied and emails the customer (EM-11), same as replying via the Admin Portal Messages tab. **Third bug found and fixed 2026-07-28 (full QA pass), same file:** `update-webhook.js` had its own hardcoded staff-domain check (`STAFF_DOMAIN` defaulting to `summitsensorygym.com` only) instead of the shared `isStaffEmail()` helper in `lib/auth.js` (default `summitsensory.com,summitsensorygym.com`). Bryan's own address (`bryan@summitsensory.com`) is on the domain the old fallback excluded — if `STAFF_EMAIL_DOMAIN` isn't set in Vercel, his own replies once this automation is registered would have been silently misread as non-staff. Fixed by importing `isStaffEmail()` instead; verified via esbuild in both repos. |
| PLAN-9 | **Refer a Friend + Photo/Video Showcase tabs** | `[FULLY LIVE 2026-07-28 — Jotform webhook + both Vercel env vars confirmed registered by Bryan]` | Two new "My Order" tabs (SCR-12/13). **Referral:** native form → EP-24 → `createReferralItem()` → standalone Referrals board (§2a-0); reward = 2% of the referred order, $25 floor/$500 cap, computed by a Monday Formula column (staff-managed), defaulting to account credit for large/institutional referrals (kickback-perception risk) with gift cards still fine for smaller individual referrals at Bryan's discretion; EM-15 notifies the team. **Showcase:** Jotform embed (form ID DS-47) + EP-25 "email me this link" button; submissions flow through the extended EP-16 webhook, which scans `rawRequest` for URL-like strings (classified image vs. video by extension) and calls `attachUgcFile()` (DS-48/49, kept in **separate** photo/video Files columns per requirement) + `incrementUgcCounts()` (DS-50…52) — reward is **$25 per 10 credits** (1 photo = 1 credit, 1 video = 2 credits, so "10 photos or 5 videos"), repeatable, with EM-16 flagging staff only when a new tier is crossed so a submission doesn't need review unless it's reward-eligible; staff does a manual ~30-second quality check (video ≥20s, different angles, ideally shows people using the frame) before approving — the app never auto-approves. Monday doesn't support counting Files-column attachments natively (confirmed: Files columns are incompatible with Formula columns), so all counting happens in `lib/monday.js`, not in Monday itself. Referrals board (`18422254966`) and the 7 DS-47…53 columns on the Manufacturing Process board were created 2026-07-15 via the monday.com MCP connector; their column IDs are hardcoded as defaults in `lib/monday.js` (same pattern as `ACCESSORY_BOARD_ID`), so no env vars need to be set for those. The "Share Your Gym" Jotform form (id `261963488177069`, `https://form.jotform.com/261963488177069`) was built 2026-07-16 via the Jotform MCP connector — 7 fields: intro/reward copy, Your Name, Your Email Address, Upload Photos (multi-file, image types), Upload Videos (multi-file, video types, 20s/angles/usage guidance in help text), optional notes, Submit. **Launch steps — confirmed done by Bryan 2026-07-28:** (1) webhook `https://portal.summitsensory.com/api/jotform/webhook` registered on this form under Settings → Integrations → Webhooks; (2) in Vercel, `JOTFORM_SHOWCASE_FORM_ID=261963488177069` set and `{"261963488177069": {"name": "Share Your Gym", "tab": "showcase"}}` merged into `JOTFORM_FORM_MAP`. Neither is independently verifiable via available tools (no MCP exposes Jotform webhook config or Vercel env var values), so this is Bryan's word, not tool-confirmed — worth watching for a real request landing on `/api/jotform/webhook` in Vercel's runtime logs the first time a customer actually submits the form, as final confirmation. All application code, Monday-side structures, and the Jotform form itself are in place. |

---

## 8. Change-request template (copy/paste when asking Claude)

```
Reference: <code, e.g. S-4.3 / DS-11 / EM-04 / SHP-2 / PLAN-2>
Type: Add | Change | Delete
What I want:
Why:
Any new Monday column IDs (from /api/monday/debug-columns):
```
